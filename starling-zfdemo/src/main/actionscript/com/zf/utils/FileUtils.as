/** * author:  Travis Hubbard * site:    http://www.zombieflambe.com * email:   tshubbard2@gmail.com * created: 24th Mar 2012 */package com.zf.utils {	import com.zf.core.Config;		public class FileUtils 	{				public static function getExtFromFilename(filename:String):String {			var dot:int = filename.indexOf('.');			return filename.slice(dot + 1, filename.length);		}				/***		 * Returns a full path based on extension		 **/		public static function getBasePathFromExt(ext:String):String {			var retPath:String = '';						switch(ext)			{				case Config.DATA_JSON:					retPath = Config.PATH_JSON;					break;									case Config.DATA_XML:					retPath = Config.PATH_XML;					break;									case Config.IMG_JPG:				case Config.IMG_PNG:				case Config.IMG_GIF:					retPath = Config.PATH_IMG;					break;			}						return retPath;		}				/***		 * Returns a filename from the full path		 **/		public static function getFilenameFromPath(path:String):String {			var pathArr:Array = path.split('/');			var fileNameArr:Array;						if(pathArr.length > 1)			{				fileNameArr = pathArr[pathArr.length - 1].split('.');			}			else 			{				fileNameArr = pathArr[0].split('.');			}						return fileNameArr[0];		}				/***		 * Returns full path		 *		 * @param String the path to the asset		 * @param Boolean if true, path will be determined from FileUtils.getBasePathFromExt, if false the full path hass been included in path param		 * @param String extension of the path		 **/		public static function getFullPath(path:String, includeAssetPath:Boolean, ext:String):String {			var fullPath:String = '';						if(includeAssetPath)  				fullPath = FileUtils.getBasePathFromExt(ext) + path;			else				fullPath = path;							return fullPath;		}	}}